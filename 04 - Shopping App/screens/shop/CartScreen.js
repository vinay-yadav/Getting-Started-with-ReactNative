import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const CartScreen = props => {
    return (
        <View>
            <Text>The CartScreen</Text>
        </View>
    );
}

const styles = StyleSheet.create({});

export default CartScreen;